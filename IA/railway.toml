[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && python train_model_light.py"

[deploy]
startCommand = "uvicorn app:app --host=0.0.0.0 --port=$PORT"
restartPolicyType = "on_failure"
