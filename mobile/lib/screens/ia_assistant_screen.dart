// Archivo conservado por compatibilidad - no se usa en la app nueva
import 'package:flutter/material.dart';

class IAAssistantScreen extends StatelessWidget {
  const IAAssistantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('No disponible')),
    );
  }
}
